package com.mealsfitbackend.Repositories;

import com.mealsfitbackend.entities.Preparation;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PreperationRepository extends JpaRepository<Preparation, Integer> {
}
